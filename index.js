function addingEventListener() {

// capturing the input 
const input = document.getElementById('input');

//adding event listener to the input variable
input.addEventListener('click',() => alert("I was Clicked!"))
}

addingEventListener()
